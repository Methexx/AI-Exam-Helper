import 'package:flutter/material.dart';

/// Global color constants for the app
class AppColors {
  // Background colors
  static const Color backgroundColor = Color(0xFF2C2C2C);
  static const Color darkBackground = Color(0xFF131313);

  // Primary brand colors
  static const Color primaryBlue = Color(0xFF2265E8);
  static const Color accentGold = Color(0xFFEFD9B0);

  // Border colors
  static const Color borderColor = Color(0xFF484848);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0B0B0);

  // Social button colors
  static const Color googleBackground = Colors.white;
  static const Color facebookBackground = Colors.white;

  // Shadow colors
  static const Color shadowBlack = Color(0xFF000000);

  // Input field colors
  static const Color inputBackground = Color(0xFF3A3A3A);
  static const Color inputBorder = Color(0xFF484848);
}
